[project]
name = "ehr-media-intelligence"
version = "0.1.0"
description = "AI-powered EHR media normalization, summarization and semantic search demo"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
